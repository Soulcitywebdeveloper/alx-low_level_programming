#include "main.h"
/**
 * mul - multiply two integers and return the result
 *
 * @a: number being multiply
 *
 * @b: number being multiply
 *
 * Return: Always 0.
 *
 */
int mul(int a, int b)
{
	return (a * b);
}
